source "https://rubygems.org"

# GitHub Pages builds this site with the github-pages gem (Jekyll 3.10 plus the
# whitelisted plugins). Using the same gem locally keeps `bundle exec jekyll serve`
# in step with production.
gem "github-pages", group: :jekyll_plugins
